
# Disk/Partition Backup Manager

Интерактивный bash-скрипт для создания полных образов дисков и разделов с поддержкой сжатия, сетевых хранилищ и гибкой системой назначений.

## Возможности

### 📀 Работа с устройствами

- **Автоматическое обнаружение** всех дисков и разделов в системе
- **Детальная информация** об устройствах: размер, файловая система, метка, точки монтирования
- **Гибкий выбор целей**: диски целиком, только разделы или смешанный режим
- **Безопасность**: проверка на запись в исходные разделы


### 💾 Места назначения

- **Локальные папки** с автоматической проверкой свободного места
- **SMB/CIFS** с поддержкой различных версий протокола (3.0, 2.1, 2.0, 1.0)
- **SSH/SSHFS** с поддержкой SSH-ключей и настраиваемых портов
- **Множественные назначения**: каждое устройство может иметь свое место назначения
- **Сохранение конфигураций**: параметры подключения сохраняются для повторного использования


### 🗜️ Сжатие и производительность

- **Автоматическое сжатие gzip** с возможностью отключения
- **Прогресс-индикаторы**: поддержка pv, dcfldd или встроенный dd
- **Детальная статистика**: время выполнения, степень сжатия, реальные размеры
- **Умная оценка размера** с учетом типа данных


### 🔄 Восстановление

- **Проверка совместимости** образа и целевого устройства
- **Защита от ошибок**: проверка монтирования, размера, типа устройства
- **Множественные источники**: восстановление из сессий бэкапа или отдельных файлов
- **Безопасное подтверждение** перезаписи устройств


### ⚙️ Система управления

- **Интерактивное меню** с цветной подсветкой
- **Сохранение настроек**: автоматическое сохранение выбранных устройств и конфигураций
- **Организация по сессиям**: автоматическое создание папок по схеме `hostname/YYYY-MM-DD_HHMMSS`
- **Управление конфигурациями**: создание, редактирование и удаление сетевых настроек


## Системные требования

### Обязательные утилиты

```bash
# Основные системные утилиты (обычно предустановлены)
lsblk dd mount umount gzip gunzip df stat blockdev awk sed grep tr

# Для Debian/Ubuntu:
sudo apt update && sudo apt install util-linux coreutils gawk

# Для CentOS/RHEL/Fedora:
sudo dnf install util-linux coreutils gawk
```


### Дополнительные утилиты (рекомендуемые)

```bash
# Для продвинутых индикаторов прогресса и сетевых функций
# Debian/Ubuntu:
sudo apt install pv dcfldd sshfs cifs-utils

# CentOS/RHEL/Fedora:
sudo dnf install pv dcfldd fuse-sshfs cifs-utils

# Arch Linux:
sudo pacman -S pv dcfldd sshfs cifs-utils
```


### Описание дополнительных утилит

- **pv** - продвинутый индикатор прогресса с ETA и скоростью передачи
- **dcfldd** - усовершенствованная версия dd с встроенным прогрессом
- **sshfs** - монтирование удаленных директорий через SSH/SFTP
- **cifs-utils** - поддержка SMB/CIFS для Windows-шар и Samba


## Установка и запуск

### Быстрый старт

```bash
# Скачивание скрипта
wget https://github.com/username/disk-backup-manager/raw/main/interactive-backup.sh
chmod +x interactive-backup.sh

# Запуск (требуются права root)
sudo ./interactive-backup.sh
```


### Ручная установка

```bash
# Клонирование репозитория
git clone https://github.com/username/disk-backup-manager.git
cd disk-backup-manager
chmod +x interactive-backup.sh

# Запуск
sudo ./interactive-backup.sh
```


## Использование

### Типичный рабочий процесс

1. **Просмотр устройств** (пункт 1) - ознакомление с доступными дисками и разделами
2. **Выбор целей** (пункт 2) - выбор дисков/разделов для бэкапа
3. **Настройка назначения** (пункт 3) - указание места сохранения образов
4. **Оценка размера** (пункт 4) - проверка требуемого места и доступного пространства
5. **Выполнение бэкапа** (пункт 5) - создание образов с прогрессом и статистикой
6. **Восстановление** (пункт 6) - при необходимости восстановления из образов

### Примеры конфигураций

#### Локальное назначение

```
Путь: /backup/disk-images/
Результат: /backup/disk-images/hostname/2025-08-31_170000/sda1_part.img.gz
```


#### SMB/CIFS назначение

```
Сервер: 192.168.1.100
Шара: backups
Пользователь: backup_user
Домен: WORKGROUP (опционально)
```


#### SSH/SSHFS назначение

```
Хост: backup-server.example.com
Пользователь: backup
Путь: /home/backup/disk-images
Порт: 22 (по умолчанию)
SSH-ключ: /root/.ssh/backup_key (опционально)
```


### Индивидуальные назначения

Скрипт позволяет настроить отдельное место назначения для каждого устройства:

- sda1 → локальная папка `/backup/system/`
- sda2 → SMB-шара `//server1/backups/`
- sdb1 → SSHFS `user@server2:/remote/backup/`


## Файловая структура

### Конфигурационные файлы

```
~/.backup_manager_config              # Основные настройки и выбранные устройства
~/.backup_manager_destinations/       # Директория с сетевыми конфигурациями
├── office-smb.conf                   # Пример SMB конфигурации
├── home-server.conf                  # Пример SSHFS конфигурации  
└── ...
```


### Структура бэкапов

```
/путь/к/назначению/
└── hostname/                         # Имя компьютера
    └── 2025-08-31_170000/           # Дата и время сессии
        ├── sda_disk.img.gz          # Образ целого диска (сжатый)
        ├── sda1_part.img.gz         # Образ раздела (сжатый)
        └── sdb2_part.img            # Образ раздела (несжатый)
```


## Безопасность

### Меры предосторожности

- **Проверка root-прав**: скрипт требует запуска от root для доступа к блочным устройствам
- **Защита от перезаписи**: автоматическая проверка на запись в исходные разделы
- **Подтверждение операций**: критичные операции требуют явного подтверждения
- **Проверка монтирования**: предотвращение восстановления на смонтированные разделы


### Хранение паролей

- Пароли сохраняются в конфигурационных файлах в открытом виде
- Файлы защищены правами 600 (только владелец)
- Рекомендуется использование SSH-ключей вместо паролей где возможно


## Совместимость

### Операционные системы

- ✅ Debian/Ubuntu и производные
- ✅ CentOS/RHEL/Fedora и производные
- ✅ Arch Linux и производные
- ✅ Astra Linux
- ✅ Любые современные Linux-дистрибутивы с systemd


### Файловые системы

- ✅ ext2/ext3/ext4
- ✅ NTFS
- ✅ FAT32/exFAT
- ✅ XFS/Btrfs
- ✅ Любые файловые системы (создается побайтовая копия)


## Ограничения

- Требуются права root для доступа к блочным устройствам
- Время выполнения зависит от размера диска и скорости I/O
- Сжатие значительно увеличивает нагрузку на CPU
- Сетевые назначения зависят от стабильности соединения


## Лицензия

MIT License



